<h2>@<%= @user.nickname %>'s Profile</h2>

<section id='user-summary' class='summary'>
    <ul>
      <li class='user-name'><%= @user.provider %> handle: @<%= @user.nickname %></li>
      <li>Member since <%= @user.created_at.strftime("%B %-m, %Y") %></li>
    </ul>

</section>
